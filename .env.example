# Database Configuration
# Get your PostgreSQL connection string from services like Neon, Supabase, or Railway
DATABASE_URL=postgresql://user:password@host:5432/freetimematcher?sslmode=require

# Server Port (optional, defaults to 3000)
PORT=3000

# Node Environment
NODE_ENV=development
